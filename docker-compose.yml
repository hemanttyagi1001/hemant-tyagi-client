services:
  client:
    build:
      context: .
      args:
        VITE_GOOGLE_MAPS_API_KEY: ${VITE_GOOGLE_MAPS_API_KEY}
    container_name: hemant-tyagi-saas-client
    ports:
      - "3001:4173"
    restart: unless-stopped
